{
  "name": "apka-dla-szymona-server-client",
  "version": "1.0.0",
  "description": "Client side of application",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "rm -rf dist && mkdir dist && cp index.html dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
  }
}
