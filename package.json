{
  "name": "apka-dla-szymona",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "install": "npm run install:client && npm run install:server",
    "install:client": "cd client && npm install",
    "install:server": "cd server && npm install",
    "start": "node dist/index.js",
    "build": "npm run build:modules && npm run build:modules:copy",
    "build:modules": "npm run build:client && npm run build:server",
    "build:client": "cd client && npm run build",
    "build:server": "cd server && npm run build",
    "build:modules:copy": "npm run build:client:copy && npm run build:server:copy",
    "build:client:copy": "cp -a client/dist/. dist",
    "build:server:copy": "cp -a server/dist/. dist"
  }
}
